---
title: "Using anatembea to Reconstruct Transmission Patterns: A Vignette"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Using anatembea to Reconstruct Transmission Patterns: A Vignette}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "images/"
)
# Skip slow model-fitting code during R CMD check
eval_model <- identical(Sys.getenv("NOT_CRAN"), "true")
```

In this vignette, we demonstrate how to use *anatembea* to reconstruct patterns of transmission intensity, seasonality, and malaria burden from monthly malaria prevalence data in children under 5 years old. Later vignettes will demonstrate applying this framework to settings where pregnant women are screened for malaria (using either rapid diagnostic tests or slide microscopy) with data available at a monthly cadence as well as exploring scenarios involving different data cadences or stratifications (e.g., by gravidity).


## Input Data

The only user input required for running *anatembea* is a data frame of monthly prevalence with at least three columns:

1.  `month`: Month (here formatted as 'yearmon', provided by the package 'zoo')
2.  `tested`: The number of individuals tested
3.  `positive`: Of those tested, the number of positive test results

In our example, the data frame `tanga_data_slim` was generated by sampling binomially from the target infection prevalence of the simulation presented above using a monthly sample size of 1000 children.

```{r data}
library(tidyverse)
library(anatembea)
library(readxl)
library(janitor)
library(epitools)
library(data.table)

sys.source(here::here("standardise_pr.R"), envir = .GlobalEnv)

final_std2 <- final_std %>% filter(yy > 1999) %>%
  mutate(year = yy,
        tested = as.integer(total_std_pop),
         positive = as.integer(expected_cases)
         ) %>% select(year, tested, positive)


Kilifi_pr_mbg <- read_excel("~/Library/CloudStorage/OneDrive-LSTM/KWTRP/Postdoc/KHDSS malaria admission/KCH admissions 1990-2024/MBG MAP Trauma TPR KHDSS_PfPR (031025).xlsx", sheet = "MBG PfPR2-10") %>% clean_names() %>% filter(yy>1989)

kilifi_trauma_pr <- read_excel("~/Library/CloudStorage/OneDrive-LSTM/KWTRP/Postdoc/KHDSS malaria admission/KCH admissions 1990-2024/data/Trauma TPR KHDSS (141025).xlsx")

Kilifi_pr_det <-  readRDS("~/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/malariasimulation/data/Kilifi_pr_det.rds")

Kilifi_pr_det_u5 <- readRDS("~/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/malariasimulation/data/Kilifi_pr_det_u5.rds")

Kilifi_sm <- readRDS("/Users/alice.kamau/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/malariasimulation/data/Kilifi_malaria_hosp.rds")

Kilifi_sm_u5 <- readRDS("/Users/alice.kamau/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/malariasimulation/data/Kilifi_malaria_adm_u5.rds")

kilifi_mbg_pr <- read_excel("~/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/anatembea/data-raw/kilifi_mbg_pr.xlsx")

KEN_PfPR_mean_long <- readRDS("~/Library/CloudStorage/OneDrive-LSTM/KWTRP/Postdoc/Age profile/PfPR/Data/KEN_PfPR_mean_long.rds")

malariasim_prev <- readRDS("~/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/anatembea/data/malariasim_prev.rds")

KEN_PfPR_mean <- KEN_PfPR_mean_long %>% 
                        dplyr::select(Admin_1, year, PfPR) %>%
                        dplyr::mutate(tested = as.integer(100),
                                      positive = as.integer(round(PfPR*100))) %>%
                        dplyr::select(-PfPR)

# Kilifi_pr_det_u5 <- Kilifi_pr_det_u5%>%
#                     dplyr::filter(YY > 1999)

kilifi_mbg_pr <- kilifi_mbg_pr %>% #filter(year > 1999) %>%
                    mutate(positive = as.integer(positive),
                           tested = as.integer(tested))
  
kilifi_trauma_pr <- kilifi_trauma_pr %>% filter(YY > 1999) 
Kilifi_pr_mbg <- Kilifi_pr_mbg %>% filter(yy > 1999) 

Kilifi_pr_det_u5 <- Kilifi_pr_det_u5 %>% filter(YY > 1999) 

Kilifi_pr_det2 <- Kilifi_pr_det%>%
                    ungroup()%>%
                    dplyr::filter(YY > 1999)%>% 
                  dplyr::select(month,positive,tested)

Kilifi_pr_det3 <- Kilifi_pr_det %>% filter(YY > 1999) %>% 
  mutate(year = YY) %>% 
  group_by(year) %>% 
  summarise(tested = sum(tested, na.rm = TRUE),
            positive = sum(positive, na.rm  = T))

Kilifi_pr_det4 <- epitools::binom.exact(Kilifi_pr_det3$positive, Kilifi_pr_det3$tested, conf.level = 0.95)

Kilifi_pr_det4 <- Kilifi_pr_det4 %>%  rowid_to_column("year") %>% mutate(year = year + 1989)

Kilifi_pr_det_u5 <- Kilifi_pr_det_u5 %>% 
  mutate(year = YY) %>% 
  group_by(year) %>% 
  summarise(tested = sum(tested, na.rm = TRUE),
            positive = sum(positive, na.rm  = T))

```

```{r, fig.show='hold'}
head(Kilifi_pr_det2, 5)
```

## Running a pMCMC fitting

The `run_pmcmc` function is the central tool to fit our model to observed data. It formats the provided data set, sets up required functions and parameters, and finally runs the pMCMC. Broadly, the flow of actions is as follows:

1.  Process provided data set, specifically formatting necessary time variables.
2.  Format and declare parameters. These include both constant or known parameter values needed for malariasimulation as well as pMCMC parameters that will be fit.
3.  Set particle filter and pMCMC settings.
4.  Run the pMCMC.
5.  Format output.

By default, in the absence of other contextual information, we use the first year’s prevalence (total number of positives in the first year divided by total number of tested in the first year) to calibrate initial pre-observation transmission and immunity, minimising potential seasonal biases of a shorter calibration window. However, *anatembea* provides functionality for the user to adjust this assumption. In the example below, we will assume that, on average before data collection, prevalence in children under 5 was 0.4, as specified in the option `target_prev`.

Other optional user parameters include pMCMC particles/step/chains, the maximum mosquito emergence rate, and treated fraction of clinical cases. No rainfall or other environmental data are required. For available options, see package [documentation](https://mrc-ide.github.io/anatembea/reference/run_pmcmc.html "run_pmcmc documentation").

```{r pmcmc, fig.show='hold',  dependson="data", eval=identical(Sys.getenv("NOT_CRAN"), "true")}

result_kilifi <- anatembea:::run_pmcmc_annual(data_raw=kilifi_mbg_pr,
                               target_prev = 0.3,
                               target_prev_group= "2to10",
                               comparison = '2to10',
                               n_particles=100,
                               n_steps = 100)


result_kilifi <- anatembea:::run_pmcmc_annual(data_raw=malariasim_prev,
                               target_prev = 0.43,
                               target_prev_group= "2to10",
                               comparison = '2to10',
                               n_particles=10,
                               n_steps = 10)


result_kilifi <- anatembea:::run_pmcmc_annual(data_raw=final_std2,
                               target_prev = 0.3,
                               target_prev_group= "2to10",
                               comparison = '2to10',
                               n_particles=100,
                               n_steps = 100)

```

## Visualising output

We can then visualise how well the pMCMC fit to the prevalence data, as well as unobserved transmission metrics like clinical incidence in the entire population and the entomological inoculation rate (EIR).

First we'll perform some simple diagnostics, including visualising the MCMC trace and calculating the acceptance rate and effective sample size.

```{r diagnostics, eval=identical(Sys.getenv("NOT_CRAN"), "true")}
measure_names <- c('prev_05',"prev_flex","clininc_014",'clininc_all','EIR','betaa')
year <- kilifi_mbg_pr$year

timelength <- length(year)
num_year <- length(result_kilifi$history[1,1,])
start_year_index <- (num_year-timelength+1)

mcmc <- result_kilifi$mcmc[,]
ar <- 1 - coda::rejectionRate(coda::as.mcmc(mcmc))
ess <- coda::effectiveSize(coda::as.mcmc(mcmc))

pars_list <- names(mcmc)
trace_plots <- lapply(pars_list, function(x){
  bayesplot::mcmc_trace(mcmc,pars = x) +
    ggtitle(paste0(x,' / AR: ',round(ar[x],3),' / ESS: ',round(ess[x],1)))+
    theme(title = element_text(size=6),
          axis.title.y = element_blank())
  })
trace_plots
```

In the example above, our diagnostics don't look great. We want an acceptance rate around 20% and an ESS of at least 200 (the higher the better). However, based on some simplified options we've given, we are unlikely to achieve good performance. Besides the data (`data_raw`) and `target_prev`, when we ran the pMCMC, we also specified two additional options, which set the number of particles (`n_particles`) and the number of MCMC iterations (`n_steps`) for the pMCMC algorithm. We gave lower numbers than advised for a real analysis to avoid lengthy computational times for this example. A good starting point for an initial run is about 200 particles and at least 1000 steps (although for a final analysis, the number of steps may be above 10,000 to ensure MCMC convergence and proper mixing).

Even though our MCMC doesn't look great, let's still visualize the model fit to the data:

```{r output_process, eval=identical(Sys.getenv("NOT_CRAN"), "true")}

history.dfs <- lapply(measure_names,function(x){
  as.data.frame(t(result_kilifi$history[x,,start_year_index:num_year]))%>%
        dplyr::mutate(year=year)%>%
    reshape2::melt(id='year')%>%
    dplyr::group_by(year)%>%
    dplyr::summarise(median=median(value),
              upper=quantile(value,probs=0.975),
              lower=quantile(value,probs=0.025))%>%
    dplyr::mutate(measure=x)
})
names(history.dfs) <- measure_names


saveRDS(history.dfs[['EIR']],"~/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/anatembea/data/Kilifi_eir.rds")

saveRDS(history.dfs[['prev_flex']],"~/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/anatembea/data/Kilifi_pr2_10.rds")

saveRDS(history.dfs[['clininc_014']],"~/Library/CloudStorage/OneDrive-LSTM/LSTM/WT work/anatembea/data/Kilifi_ci.rds")

prev_plot <- ggplot(data = history.dfs[["prev_flex"]]) +
  geom_ribbon(aes(x = year, ymin = lower, ymax = upper), fill = "#A6CEE3") +
  geom_line(aes(x = year, y = median, colour = "anatembea")) +
  # geom_point(data = kilifi_trauma_pr, aes(x = YY, y = PR, colour = "IPD 1m-14y")) +
  # geom_point(data = kilifi_trauma_pr,aes(x = YY, y = PR_Smith, colour = "IPD 2-10")) +
  # geom_point(data = final_std2,aes(x = year, y = positive/tested, colour = "std PR")) +
  # geom_errorbar(data = kilifi_trauma_pr,aes(x = YY, ymin = lower, ymax = upper),
  #               width = 0.2, alpha = 0.5,linetype = "dashed",colour = "red") +
  geom_point(data = Kilifi_pr_mbg, aes(x = yy, y = median, colour = "community MBG")) +
  geom_errorbar(data = Kilifi_pr_mbg,aes(x = yy,ymin = x95_percent_lci,ymax = x95_percent_uci),
                width = 0.2,alpha = 0.5,linetype = "dashed") +
  scale_colour_manual(
    name = "",
    values = c(
      "anatembea" = "#1F78B4",
      "IPD 1m-14y" = "purple",
      "IPD 2-10" = "red",
      "std PR" = "orange",
      "community MBG" = "black")) +
  scale_x_continuous(breaks = seq(1990, 2025, 5),expand = c(0, 0)) +
  scale_y_continuous(limits = c(0, 0.6),expand = c(0, 0) ) +
  labs(y = "Prevalence in 2 to 10 years",colour = "Data source") +
  theme_minimal() +
  theme(axis.title.x = element_blank()) +
   theme(
    legend.position = c(0.80, 0.85),
    legend.background = element_rect(
      fill = scales::alpha("white", 0.7),
      colour = "grey70"), legend.key = element_blank())

prev_plot2 <- ggplot(data = history.dfs[["prev_05"]]) +
  geom_ribbon(aes(x = year, ymin = lower, ymax = upper), fill = "pink") +
  geom_line(aes(x = year, y = median, colour = "anatembea")) +
  geom_point(data = Kilifi_pr_det_u5, aes(x = year, y = positive/tested, colour = "IPD 1m-5y")) +
  scale_colour_manual(
    name = "",
    values = c(
      "anatembea" = "purple",
      "IPD 1m-5y" = "black")) +
  scale_x_continuous(breaks = seq(1990, 2025, 5),expand = c(0, 0)) +
  scale_y_continuous(limits = c(0, 0.5),expand = c(0, 0) ) +
  labs(y = "Prevalence in 2 to 10 years",colour = "Data source") +
  theme_minimal() +
  theme(axis.title.x = element_blank()) +
   theme(
    legend.position = c(0.80, 0.85),
    legend.background = element_rect(
      fill = scales::alpha("white", 0.7),
      colour = "grey70"), legend.key = element_blank())


inc_plot <- ggplot(data=history.dfs[['clininc_014']])+
  geom_ribbon(aes(x=year,ymin=lower*365,ymax=upper*365),fill="orange")+
  geom_line(aes(x=year,y=median*365),color="brown")+
  #geom_line(data=Kilifi_sm,aes(x=YY,y=rate/12),color='black')+
  scale_y_continuous(limits=c(0,NA),expand=c(0,0))+
  labs(y='Clinical Incidence 0- 14 years per year')+
  theme_minimal()+
  theme(axis.title.x = element_blank())

inc_plot2 <- ggplot(data=history.dfs[['clininc_all']])+
  geom_ribbon(aes(x=year,ymin=lower,ymax=upper),fill="orange")+
  geom_line(aes(x=year,y=median),color="brown")+
  scale_y_continuous(limits=c(0,NA),expand=c(0,0))+
  labs(y='Clinical Incidence,\nall ages')+
  theme_minimal()+
  theme(axis.title.x = element_blank())

summary(Kilifi_sm)
summary(history.dfs[['clininc_014']])

sev_plot <- ggplot(data=history.dfs[['clininc_014']])+
  geom_ribbon(aes(x=year,ymin=lower/0.0006,ymax=upper/0.0006),fill="#B2DF8A")+
  geom_line(aes(x=year,y=median/0.0006),color="#33A02C")+
  geom_line(data=Kilifi_sm,aes(x=YY,y=rate/0.003),color='black', linetype = "dashed")+
  scale_x_continuous(breaks=seq(1990,2025,5),expand=c(0,0))+
  scale_y_continuous(limits=c(0,NA),expand=c(0,0))+
  labs(y='Clinical incidence aged 0- 14 years')+
  theme_minimal()+
  theme(axis.title.x = element_blank())


sev_obs <- ggplot(data=Kilifi_sm) +
  geom_ribbon(aes(x=YY,ymin=lower,ymax=upper),fill="#B2DF8A") +
  geom_line(aes(x=YY,y=rate),color='#33A02C') +
  scale_x_continuous(breaks=seq(1990,2025,5),expand=c(0,0)) +
  scale_y_continuous(limits=c(0,NA),expand=c(0,0)) +
  labs(y='Observed severe incidence (1m- 14 years)')+
  theme_minimal()+
  theme(axis.title.x = element_blank())


eir_plot <- ggplot(data=history.dfs[['EIR']])+
  geom_ribbon(aes(x=year,ymin=lower,ymax=upper),fill="#FB9A99")+
  geom_line(aes(x=year,y=median),color="#E31A1C")+
  scale_x_continuous(breaks=seq(1990,2025,5),expand=c(0,0))+
  scale_y_log10(expand=c(0,0)) +
 # scale_y_log10(
 #    limits = c(0.000001, 30),
 #    breaks = c(0.000001, 0.3, 1, 3, 10, 30),
 #    labels = c("0.01", "0.3", "1", "3", "10", "30"),
 #    expand = c(0, 0)
 #  ) +
  labs(y='EIR')+
  theme_minimal()+
  theme(axis.title.x = element_blank())


beta_plot <- ggplot(data=history.dfs[['betaa']])+
  geom_ribbon(aes(x=year,ymin=lower*365,ymax=upper*365),fill="#FB9A99")+
  geom_line(aes(x=year,y=median*365),color="#E31A1C")+
  scale_x_continuous(breaks=seq(1990,2025,5),expand=c(0,0))+
  scale_y_continuous(limits=c(0,NA),expand=c(0,0)) +
  labs(y='betaa')+
  theme_minimal()+
  theme(axis.title.x = element_blank())

combined_ggplot <- cowplot::plot_grid(prev_plot,prev_plot2,inc_plot,sev_plot,eir_plot,ncol=1)
# combined_ggplot <- prev_plot+inc_plot+eir_plot+plot_layout(ncol=1)
result_kilifi$output <- combined_ggplot
library(ggpubr)

prev_plot  <- prev_plot + theme(plot.margin = margin(10, 10, 10, 40))
prev_plot2 <- prev_plot2 + theme(plot.margin = margin(10, 10, 10, 40))
inc_plot  <- inc_plot + theme(plot.margin = margin(10, 10, 10, 40))
sev_plot <- sev_plot + theme(plot.margin = margin(10, 10, 10, 40))
eir_plot <- eir_plot + theme(plot.margin = margin(10, 10, 10, 40))

figures <- ggarrange(prev_plot,prev_plot2,inc_plot,sev_plot,eir_plot,
                     ncol = 1, nrow = 5,
                     align = "v",         # align vertically
                     widths = c(1)
) 

result_kilifi$figures <- figures


```

```{r, fig.show='hold', eval=identical(Sys.getenv("NOT_CRAN"), "true")}
result_kilifi$output

result_kilifi$figures 


```
